spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/HW1?characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 1234abcd
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    database: mysql
    show-sql: false
    hibernate:
      ddl-auto: update
      naming:
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyJpaCompliantImpl
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL5Dialect
      open-in-view: true